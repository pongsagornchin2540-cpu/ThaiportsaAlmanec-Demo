# Archived assets (not in production)

These files were removed from the live Version2 stylesheet path to reduce
multi-skin confusion. The active design system is:

- `site-foundation.css` (source of truth — load last)
- `visitor-gateway.css` + `portal.css` + page modules
- `cinematic.css` (shared nav / motion primitives for now)

| File | Reason |
|------|--------|
| editorial.css | Legacy editorial skin; unused by live HTML |
| editorial-backup.css | Never linked |
| style.css | Pre-gateway prototype styles |
| index.editorial-backup.html | Old homepage backup |

Do not link these from production pages.
